requests>=2.31
Pillow>=10.0
python-dotenv>=1.0
numpy>=1.24

# Только для tests/ (pytest tests/) — в проде пайплайну не нужен.
pytest>=8.0

# Опционально — 2.5D-параллакс в pipeline_smart.py (PARALLAX=0 отключает).
# Без них скрипт работает как обычно, просто без этой фичи.
# opencv-python-headless
# torch --index-url https://download.pytorch.org/whl/cpu
# torchvision --index-url https://download.pytorch.org/whl/cpu
# transformers

# Опционально — точная привязка кадров к речи в pipeline_smart.py
# (WHISPER_ALIGN=1 включает; без переменной или без пакета — прежняя оценка
# по словам, пайплайн не падает). CPU-инференс, модель "small" по умолчанию
# (WHISPER_MODEL=... меняет размер, WHISPER_LANG=... язык).
# faster-whisper
