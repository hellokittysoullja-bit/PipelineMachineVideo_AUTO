requests>=2.31
Pillow>=10.0
python-dotenv>=1.0
numpy>=1.24

# Только для tests/ (pytest tests/) — в проде пайплайну не нужен.
pytest>=8.0

# opencv-python-headless — используется в pipeline_smart.py для 2.5D-параллакса
# (опционально, PARALLAX=0 отключает) И для anchor-aware кропа по лицу
# (detect_face_anchor — всегда, если cv2 доступен; без него просто старый
# центр-кроп, без регрессии). Пин на 4.10.0.84 обязателен: opencv-python-headless
# 5.0.0 (то, что подтягивается без пина) идёт БЕЗ CascadeClassifier и БЕЗ файлов
# haarcascade вообще (проверено вживую) — anchor-по-лицу молча никогда не
# сработает на такой версии.
opencv-python-headless==4.10.0.84
# Опционально — для 2.5D-параллакса (Depth-Anything-V2) И для CLIP-проверки
# релевантности медиа (clip_relevance() в pexels_photo() — ловит кандидатов,
# которые прошли текстовый поиск Pexels по ключевым словам, но по смыслу
# мимо запроса; CLIP_RELEVANCE=0 отключает). Без torch/transformers — обе
# фичи просто выключены (безопасный откат, без регрессии). torchvision
# нужен только параллаксу, CLIP его не использует, но ставить всё вместе
# проще, чем разводить на два опциональных набора.
# torch --index-url https://download.pytorch.org/whl/cpu
# torchvision --index-url https://download.pytorch.org/whl/cpu
# transformers
#
# Стек, на котором откалиброваны пороги отбора звука и снят золотой набор
# картинок: torch 2.14.0+cpu, transformers 5.17.0. Это НЕ жёсткий пин
# (обе зависимости опциональные, ставятся отдельно) — это записанный факт:
# дрейф уже случался вживую и сдвинул кадр золотого набора через порог без
# единой правки кода. Канарейка tests/test_clap_canary.py ловит такой сдвиг
# на замороженных отрезках и называет обе версии в тексте провала, чтобы
# «поменялась модель» не выглядело как «сломался код».

# НЕ опционально, вопреки прежней записи здесь — 18.09 измерено, что именно
# отваливается без него. Jina CLIP v2 обслуживает ДВА разных слоя, и
# «опциональным» был честно назван только второй:
#   1) semantic_query_assignment() — раздача АВТОРСКИХ запросов блокам ПО
#      СМЫСЛУ фразы. Включена по умолчанию (SEMANTIC_QUERY_ASSIGNMENT), и без
#      onnxruntime падает fail-open в ПОЗИЦИОННУЮ раздачу по кругу, то есть в
#      ровно то поведение, ради отмены которого функция и написана (её же
#      докстринг: 6 промахов из 8 на реальном хуке — фраза про кино получала
#      запрос про весы). Снаружи это неотличимо от «картинки поставили в
#      рандом»: ни одной строки в логе не было, пока 18.09 не появился
#      media_plan/capability_report.json.
#   2) sentence_relevance() — ensemble SigLIP2+Jina (VISUAL_DIRECTOR_MODE=
#      shadow/assist, сам по себе off по умолчанию). Здесь откат на SigLIP2
#      solo действительно мягкий, как и было написано.
# Цена замерена на масштабе настоящего эпизода, а не оценена: 142 блока x 42
# запроса = 51.7с за прогон (модель уже прогрета; dummy-батч vision-башни
# ужат до 1, см. _jina_text_emb_batch). Минута на эпизод против случайной
# раздачи запросов — не тот обмен, который стоит оставлять на волю
# окружения. huggingface_hub не пиновать отдельно — уже тянется transformers.
onnxruntime>=1.20

# Опционально — коррекция ударений известных омографов (scripts/
# stress_placement.py, используется только из speech_planner.py при
# STRESS_HINTS_ENABLED=1, само по себе выключено по умолчанию). Без
# ruaccent — fail-open, подсказки просто не генерируются (см. докстринг
# homograph_hints_for_text() в speech_planner.py), планирование работает
# как раньше.
# ruaccent>=1.5

# Опционально — библиотека настоящих звуков (scripts/sound_library.py build):
# отбор полевых записей и эффектов по описанию (CLAP laion/larger_clap_general)
# и вето по классам AudioSet (AST MIT/ast-finetuned-audioset). Те же
# torch/transformers, что выше; без них команда build не запустится, а
# рендер с уже собранной библиотекой работает без моделей.
